function.getactivetaskidsforprocess.description=Gets the IDs of active tasks (Assigned and Accepted) for a process and, optionally, its sub processes.
function.getactivetaskidsforprocess.param.processID.description=Process to query
function.getactivetaskidsforprocess.param.includeSubs.description=Include sub processes
